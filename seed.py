"""Стартовое наполнение портала «Курсоград»: преподаватели, курсы и уроки.

Текст урока хранится в лёгкой разметке, которую разбирает фильтр
``lesson_html`` в app.py:

* пустая строка разделяет блоки;
* ``## Заголовок`` — подзаголовок раздела;
* строки, начинающиеся с ``- `` или ``1. ``, — маркированный/нумерованный список;
* блок между строками из трёх обратных кавычек — пример кода;
* внутри текста `код` и **выделение**.
"""

SEED_TEACHERS = [
    ("Ирина Соколова", "Веб-разработка",
     "10 лет во фронтенде, преподаёт HTML, CSS и JavaScript с нуля."),
    ("Павел Дорохов", "Python и данные",
     "Инженер данных, автор курсов по Python, SQL и аналитике."),
    ("Мария Ким", "Дизайн интерфейсов",
     "Продуктовый дизайнер, специализация — UX-исследования и прототипы."),
    ("Артём Нечаев", "Иностранные языки",
     "Преподаватель английского, готовит к международным экзаменам."),
]

SEED_COURSES = [
    ("Основы HTML и CSS", "Веб-разработка", "Начальный", 24, 1,
     "Вёрстка страниц с нуля: теги, блочная модель, флексбокс и адаптивность."),
    ("JavaScript для начинающих", "Веб-разработка", "Начальный", 36, 1,
     "Переменные, функции, DOM и первые интерактивные интерфейсы."),
    ("Python: первый язык", "Программирование", "Начальный", 40, 2,
     "Синтаксис, структуры данных, функции и работа с файлами."),
    ("SQL и базы данных", "Аналитика", "Средний", 30, 2,
     "Запросы SELECT, соединения, агрегации и проектирование таблиц."),
    ("Аналитика данных на Python", "Аналитика", "Средний", 48, 2,
     "Pandas, визуализация и построение отчётов по реальным датасетам."),
    ("UX/UI: основы дизайна", "Дизайн", "Начальный", 28, 3,
     "Принципы композиции, сетки, типографика и прототипирование."),
    ("Исследование пользователей", "Дизайн", "Продвинутый", 22, 3,
     "Интервью, CustDev, юзабилити-тесты и анализ результатов."),
    ("Английский для IT", "Языки", "Средний", 32, 4,
     "Техническая лексика, чтение документации и общение в команде."),
]

# course_id -> [(заголовок урока, текст урока), ...]
SEED_LESSONS = {
    # ------------------------------------------------ 1. Основы HTML и CSS
    1: [
        ("Как устроена веб-страница", """
Каждая веб-страница — это обычный текстовый файл с расширением `.html`. Браузер читает его сверху вниз, строит из тегов дерево элементов (DOM) и отрисовывает результат на экране. За структуру отвечает HTML, за внешний вид — CSS, за поведение — JavaScript.

Документ начинается с объявления `<!DOCTYPE html>`, за которым идёт корневой элемент `<html>`. Внутри него две части: `<head>` со служебной информацией (кодировка, заголовок вкладки, подключённые стили) и `<body>` — всё, что видит пользователь.

## Пример

```
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="utf-8">
  <title>Моя первая страница</title>
</head>
<body>
  <h1>Привет, Курсоград!</h1>
  <p>Это мой первый абзац.</p>
</body>
</html>
```

## Практическое задание

1. Создайте файл `index.html` и скопируйте в него пример выше.
2. Откройте файл в браузере двойным щелчком — убедитесь, что заголовок вкладки совпадает с содержимым `<title>`.
3. Нажмите F12 и найдите во вкладке «Elements» («Инспектор») элемент `<h1>`. Попробуйте изменить его текст прямо в инструментах разработчика.

## Запомнить

- HTML — структура, CSS — оформление, JavaScript — поведение.
- В `<head>` — служебная информация, в `<body>` — видимое содержимое.
- Инструменты разработчика (F12) — главный помощник верстальщика.
"""),
        ("Основные теги HTML", """
Тегов в HTML больше сотни, но в повседневной вёрстке используется пара десятков. Заголовки `<h1>`–`<h6>` задают иерархию текста (на странице должен быть один `<h1>`), абзацы оформляются тегом `<p>`, ссылки — `<a href="...">`, изображения — `<img src="..." alt="...">`.

Списки бывают маркированные `<ul>` и нумерованные `<ol>`, каждый пункт — `<li>`. Для группировки блоков используют `<div>`, для строчных фрагментов — `<span>`. Семантические теги `<header>`, `<nav>`, `<main>`, `<section>`, `<article>` и `<footer>` не меняют внешний вид, но объясняют браузеру и поисковикам, какой смысл у каждой части страницы.

Атрибуты уточняют поведение элемента: `href` — адрес ссылки, `src` — путь к картинке, `alt` — текстовое описание изображения, `class` и `id` — «имена» для стилей и скриптов.

## Пример

```
<header>
  <nav>
    <a href="/">Главная</a>
    <a href="/courses">Курсы</a>
  </nav>
</header>
<main>
  <article>
    <h1>Как выбрать первый курс</h1>
    <p>Начните с <strong>основ</strong> — они пригодятся везде.</p>
    <ul>
      <li>Определите цель</li>
      <li>Выделите время</li>
    </ul>
    <img src="plan.png" alt="План обучения">
  </article>
</main>
```

## Практическое задание

1. Сверстайте страницу-визитку: заголовок с вашим именем, абзац «О себе», список из трёх навыков и ссылку на любимый сайт.
2. Добавьте фотографию или любую картинку с осмысленным атрибутом `alt`.
3. Оберните части страницы в семантические теги `<header>`, `<main>` и `<footer>`.
"""),
        ("Селекторы и свойства CSS", """
CSS описывает, как выглядят элементы. Правило состоит из селектора (кого оформляем) и набора объявлений «свойство: значение». Стили подключают отдельным файлом через `<link rel="stylesheet" href="style.css">` — так их можно переиспользовать на всех страницах сайта.

Основные селекторы: по тегу (`p`), по классу (`.card`), по идентификатору (`#logo`). Их можно комбинировать: `.card p` — абзацы внутри карточки, `a:hover` — ссылка под курсором. Если к элементу подходит несколько правил, побеждает более специфичное, а при равной специфичности — то, что написано ниже.

Каждый элемент — прямоугольник с содержимым, внутренними отступами (`padding`), рамкой (`border`) и внешними отступами (`margin`). Это блочная модель, и понимать её обязательно: именно она объясняет, почему блоки «не влезают» или «разъезжаются».

## Пример

```
/* style.css */
body { font-family: Arial, sans-serif; color: #16203a; }
h1 { font-size: 32px; margin-bottom: 8px; }
.card {
  padding: 20px;
  border: 1px solid #e2e7f2;
  border-radius: 12px;
  background: #fff;
}
.card p { color: #67718c; }
a:hover { text-decoration: underline; }
```

## Практическое задание

1. Подключите к странице-визитке из прошлого урока файл `style.css`.
2. Оформите блок «О себе» как карточку: фон, рамка, скруглённые углы и внутренние отступы.
3. Откройте инструменты разработчика и на вкладке «Computed» («Вычислено») посмотрите, как складываются `padding`, `border` и `margin` вашей карточки.
"""),
        ("Флексбокс и сетки", """
Долгие годы блоки выстраивали в ряд с помощью хитростей вроде `float`. Сегодня для раскладки есть два удобных инструмента: Flexbox — для одномерных рядов и колонок, и CSS Grid — для двумерных сеток.

Флексбокс включается свойством `display: flex` у родителя. После этого дети становятся флекс-элементами: их можно выровнять по главной оси (`justify-content`), по поперечной (`align-items`), задать промежутки (`gap`) и разрешить перенос на новую строку (`flex-wrap: wrap`).

Grid включается через `display: grid`. Свойство `grid-template-columns` задаёт колонки: например, `repeat(3, 1fr)` — три равные колонки, а `repeat(auto-fill, minmax(250px, 1fr))` — столько колонок шириной не меньше 250px, сколько поместится. Именно так собран каталог курсов на этом портале.

## Пример

```
/* шапка: логотип слева, меню справа */
.header { display: flex; justify-content: space-between; align-items: center; }

/* карточки курсов: адаптивная сетка */
.grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
  gap: 18px;
}
```

## Практическое задание

1. Сверстайте шапку сайта: логотип слева, три ссылки меню справа, всё выровнено по вертикальному центру.
2. Ниже разместите шесть карточек в сетке из трёх колонок с отступом 16px.
3. Сузьте окно браузера и посмотрите, как ведут себя карточки. Замените `repeat(3, 1fr)` на `repeat(auto-fill, minmax(220px, 1fr))` и сравните.
"""),
        ("Адаптивная вёрстка", """
Больше половины посетителей открывают сайты с телефона, поэтому страница должна одинаково хорошо выглядеть на экране шириной и 360, и 1920 пикселей. Такой подход называется адаптивной вёрсткой.

Первое, без чего ничего не заработает, — мета-тег `<meta name="viewport" content="width=device-width, initial-scale=1">` в `<head>`. Без него мобильный браузер притворится широким экраном и просто уменьшит страницу.

Дальше в дело вступают медиазапросы: блок `@media (max-width: 760px) { ... }` применяется, только когда окно уже 760px. В нём меняют число колонок, размер шрифта, скрывают второстепенные элементы. Хорошая практика — подход mobile first: сначала описываем стили для маленького экрана, а медиазапросами с `min-width` добавляем сложность для больших.

Помогают и относительные единицы: `%`, `rem`, `vw`, а также `max-width` для картинок и контейнеров, чтобы ничего не вылезало за край экрана.

## Пример

```
img { max-width: 100%; height: auto; }
.two-col { display: grid; grid-template-columns: 2fr 1fr; gap: 20px; }

@media (max-width: 760px) {
  .two-col { grid-template-columns: 1fr; }
  h1 { font-size: 24px; }
  .nav { flex-direction: column; }
}
```

## Практическое задание

1. Добавьте на страницу мета-тег viewport и проверьте её в режиме эмуляции телефона (в инструментах разработчика — иконка с телефоном).
2. Сделайте так, чтобы двухколоночный макет на экранах уже 760px превращался в одну колонку.
3. Убедитесь, что при ширине 360px не появляется горизонтальная прокрутка. Если появляется — найдите виновника через инспектор и исправьте.

## Итог курса

Вы умеете собирать структуру страницы на HTML, оформлять её через CSS, раскладывать блоки флексбоксом и сеткой и подстраивать макет под любой экран. Следующий шаг — оживить страницу: этому посвящён курс «JavaScript для начинающих».
"""),
    ],
    # ------------------------------------------ 2. JavaScript для начинающих
    2: [
        ("Переменные и типы данных", """
JavaScript — язык, на котором «оживают» веб-страницы: он реагирует на клики, проверяет формы, подгружает данные. Код можно писать прямо в консоли браузера (F12 → Console) или подключать файлом через `<script src="app.js"></script>`.

Данные хранятся в переменных. Объявляйте их через `let` (значение можно менять) или `const` (нельзя переприсвоить). Устаревшее `var` лучше не использовать. Имена пишутся в стиле camelCase: `userName`, `totalPrice`.

Основные типы: числа (`42`, `3.14`), строки (`"текст"`, `'текст'`, шаблонные строки в обратных кавычках с подстановками `${...}`), булевы значения `true` / `false`, а также `null` и `undefined`. Оператор `typeof` показывает тип значения. Помните, что `+` для строк — это склейка: `"2" + 2` даст `"22"`.

## Пример

```
const courseName = "JavaScript для начинающих";
let lessonsDone = 0;
lessonsDone = lessonsDone + 1;

const hours = 36;
const message = `Курс «${courseName}»: ${hours} часов, пройдено уроков: ${lessonsDone}`;
console.log(message);
console.log(typeof hours, typeof courseName, typeof true);
```

## Практическое задание

1. Откройте консоль браузера и объявите три переменные: ваше имя, возраст и признак «учусь на Курсограде» (булево значение).
2. Соберите из них одно предложение с помощью шаблонной строки и выведите его через `console.log`.
3. Проверьте, что вернут выражения `"5" + 3`, `"5" - 3` и `typeof null`. Объясните себе результат.
"""),
        ("Условия и циклы", """
Программа редко выполняется строго по прямой. Условный оператор `if / else if / else` выбирает ветку в зависимости от логического выражения. Сравнивайте значения строгим `===` (без приведения типов), а не `==`. Условия комбинируются операторами `&&` (и), `||` (или), `!` (не).

Циклы повторяют действия. `for` удобен, когда известно число итераций; `while` — когда повторяем «пока верно условие»; `for...of` перебирает элементы массива без счётчика. Внутри цикла `break` прерывает его целиком, а `continue` пропускает текущую итерацию.

## Пример

```
const scores = [4, 5, 2, 5, 3];
let excellent = 0;

for (const score of scores) {
  if (score === 5) {
    excellent++;
  } else if (score < 3) {
    console.log("Есть над чем поработать");
  }
}

let i = 1;
while (i <= 3) {
  console.log(`Попытка ${i}`);
  i++;
}

console.log(excellent > 1 ? "Отличник!" : "Продолжаем учиться");
```

## Практическое задание

1. Напишите код, который выводит числа от 1 до 20, но вместо чисел, кратных 3, печатает слово «Курсо», кратных 5 — «град», а кратных 15 — «Курсоград».
2. Дан массив оценок `[5, 3, 4, 5, 2]`. Посчитайте среднее и выведите «Зачёт», если оно не меньше 3.5, иначе — «Пересдача».
3. С помощью `while` найдите первое число больше 100, которое делится на 7 без остатка.
"""),
        ("Функции", """
Функция — именованный блок кода, который можно вызывать многократно. Она принимает параметры, что-то делает и возвращает результат через `return`. Функции помогают не повторяться и делить программу на понятные части.

Есть несколько способов объявить функцию: классический `function sum(a, b) { ... }`, функциональное выражение и стрелочные функции `(a, b) => a + b` — они короче и часто используются как колбэки. Параметрам можно задать значения по умолчанию.

Переменные, объявленные внутри функции, снаружи не видны — это область видимости. Функции в JavaScript — полноценные значения: их можно передавать в другие функции, например в `array.map()` или `array.filter()`.

## Пример

```
function progress(done, total = 1) {
  if (total === 0) return 0;
  return Math.round(done / total * 100);
}

const greet = (name) => `Привет, ${name}!`;

console.log(progress(3, 5));   // 60
console.log(greet("Ирина"));   // Привет, Ирина!

const hours = [24, 36, 40, 30];
const longCourses = hours.filter((h) => h > 30);
const labels = hours.map((h) => `${h} ч`);
console.log(longCourses, labels);
```

## Практическое задание

1. Напишите функцию `isEven(n)`, которая возвращает `true` для чётных чисел.
2. Напишите функцию `formatLesson(position, title)`, возвращающую строку вида «Урок 3. Функции».
3. С помощью `map` превратите массив `["html", "css", "js"]` в массив строк в верхнем регистре, а с помощью `filter` оставьте только те, что длиннее двух символов.
"""),
        ("Работа с DOM", """
DOM (Document Object Model) — это представление HTML-страницы в виде дерева объектов, к которому у JavaScript есть доступ через глобальный объект `document`. Меняя DOM, мы меняем то, что видит пользователь, без перезагрузки страницы.

Найти элементы можно методами `document.querySelector(селектор)` — вернёт первый подходящий элемент — и `document.querySelectorAll(селектор)` — вернёт все. Селекторы те же, что в CSS.

У найденного элемента можно менять текст (`textContent`), HTML-содержимое (`innerHTML`, осторожно с пользовательскими данными), атрибуты (`setAttribute`), классы (`classList.add / remove / toggle`) и стили (`style.color`). Новые элементы создаются через `document.createElement` и добавляются методом `append`.

## Пример

```
<ul id="lessons"></ul>
<script>
  const lessons = ["Переменные", "Условия", "Функции", "DOM"];
  const list = document.querySelector("#lessons");

  for (const title of lessons) {
    const li = document.createElement("li");
    li.textContent = title;
    list.append(li);
  }

  const first = document.querySelector("#lessons li");
  first.classList.add("done");
  first.style.color = "green";
</script>
```

## Практическое задание

1. Создайте страницу с заголовком `<h1 id="title">` и пустым списком `<ul>`. Скриптом поменяйте текст заголовка на название вашего любимого курса.
2. Из массива строк сгенерируйте пункты списка, как в примере.
3. Каждому чётному пункту добавьте класс `highlight` и опишите его в CSS (например, серый фон).
"""),
        ("События и обработчики", """
Интерактивность строится на событиях: клик, ввод текста, отправка формы, прокрутка. Чтобы отреагировать на событие, к элементу добавляют обработчик — функцию, которую браузер вызовет в нужный момент: `element.addEventListener("click", handler)`.

В обработчик передаётся объект события. Из него можно узнать, что произошло (`event.type`), с каким элементом (`event.target`), какая клавиша нажата (`event.key`). Метод `event.preventDefault()` отменяет действие по умолчанию — например, не даёт форме перезагрузить страницу.

Обычная схема: находим элемент → вешаем обработчик → внутри читаем данные и меняем DOM. Именно так работают счётчики, вкладки, валидация форм и почти любой интерфейс.

## Пример

```
<input id="name" placeholder="Ваше имя">
<button id="hello">Поздороваться</button>
<p id="out"></p>

<script>
  const input = document.querySelector("#name");
  const button = document.querySelector("#hello");
  const out = document.querySelector("#out");

  button.addEventListener("click", () => {
    const name = input.value.trim();
    out.textContent = name ? `Привет, ${name}!` : "Введите имя";
  });

  input.addEventListener("keydown", (event) => {
    if (event.key === "Enter") button.click();
  });
</script>
```

## Практическое задание

1. Сделайте счётчик: число на экране и две кнопки «+» и «−». По клику значение меняется.
2. Добавьте поле ввода и кнопку «Добавить урок»: введённый текст должен появляться новым пунктом списка, а поле — очищаться.
3. Повесьте на форму обработчик `submit`, отмените перезагрузку через `preventDefault()` и покажите сообщение об ошибке, если поле пустое.

## Итог курса

Вы освоили базовый синтаксис JavaScript, научились работать с DOM и событиями — этого достаточно, чтобы делать интерактивные страницы. Дальше можно изучать `fetch` и работу с сервером, а также фреймворки вроде Vue или React.
"""),
    ],
    # ------------------------------------------------ 3. Python: первый язык
    3: [
        ("Установка Python и первый скрипт", """
Python — язык с простым синтаксисом, который используют в веб-разработке, анализе данных, автоматизации и обучении программированию. Установите последнюю версию с сайта python.org (на Windows отметьте галочку «Add Python to PATH») и проверьте в терминале командой `python --version`.

Код можно выполнять двумя способами: в интерактивном режиме (введите `python` в терминале и пишите команды построчно) или сохранив программу в файл с расширением `.py` и запустив `python hello.py`. Для удобной работы поставьте редактор — VS Code или PyCharm.

В Python отступы — не украшение, а часть синтаксиса: вложенные блоки выделяются четырьмя пробелами. Комментарии начинаются с `#`. Ввод от пользователя читает функция `input()`, вывод делает `print()`.

## Пример

```
# hello.py
name = input("Как вас зовут? ")
age = int(input("Сколько вам лет? "))

print(f"Привет, {name}!")
if age >= 18:
    print("Добро пожаловать на взрослые курсы.")
else:
    print(f"Через {18 - age} лет откроются все курсы.")
```

## Практическое задание

1. Установите Python и убедитесь, что `python --version` показывает версию 3.10 или новее.
2. Сохраните пример в файл `hello.py`, запустите и проверьте обе ветки условия.
3. Напишите скрипт-калькулятор: спросите два числа и выведите их сумму, разность, произведение и частное. Обратите внимание, чем отличаются `/` и `//`.
"""),
        ("Списки, словари, кортежи", """
Реальные программы работают не с одиночными значениями, а с коллекциями. В Python их несколько, и важно понимать, когда какую использовать.

Список (`list`) — упорядоченный изменяемый набор: `courses = ["HTML", "Python"]`. Элементы доступны по индексу с нуля, отрицательные индексы считают с конца, срезы `a[1:3]` вырезают часть. Методы `append`, `insert`, `remove`, `sort` меняют список на месте.

Кортеж (`tuple`) — такой же упорядоченный набор, но неизменяемый: `point = (10, 20)`. Его используют для данных, которые не должны меняться, и как ключ словаря.

Словарь (`dict`) хранит пары «ключ → значение»: `hours = {"HTML": 24, "Python": 40}`. Поиск по ключу мгновенный; метод `get` возвращает значение по умолчанию вместо ошибки; `items()` перебирает пары. Множество (`set`) хранит только уникальные значения и удобно для проверки вхождения.

## Пример

```
courses = ["Основы HTML и CSS", "Python: первый язык", "SQL и базы данных"]
courses.append("UX/UI: основы дизайна")
print(courses[0], courses[-1], len(courses))

hours = {"Основы HTML и CSS": 24, "Python: первый язык": 40}
hours["SQL и базы данных"] = 30
for title, h in hours.items():
    print(f"{title}: {h} ч")
print(hours.get("Английский для IT", "нет данных"))

level = ("Начальный", 1)
print(level[0])

tags = {"python", "sql", "python"}
print(tags, "sql" in tags)
```

## Практическое задание

1. Создайте список из пяти любимых блюд, отсортируйте его по алфавиту и выведите первые три элемента срезом.
2. Заведите словарь «предмет → оценка» для четырёх предметов и посчитайте средний балл.
3. Дан список с повторяющимися городами. Получите из него множество уникальных городов и выведите их количество.
"""),
        ("Функции и модули", '''
Функция объявляется словом `def`, принимает параметры и возвращает результат через `return`. Параметры могут иметь значения по умолчанию, а вызывать функцию можно с именованными аргументами — так код читается лучше: `progress(done=3, total=5)`.

Хороший тон — писать к функции докстринг (строку документации сразу после `def`) и добавлять аннотации типов: `def progress(done: int, total: int) -> int`. Они не проверяются при запуске, но помогают редактору и коллегам.

Когда файл разрастается, код делят на модули — обычные `.py`-файлы, которые подключают через `import`. В стандартной библиотеке Python сотни готовых модулей: `math`, `random`, `datetime`, `json`, `os`. Сторонние пакеты ставятся командой `pip install имя` и подключаются так же.

## Пример

```
# utils.py
def progress(done: int, total: int) -> int:
    """Процент пройденных уроков, округлённый до целого."""
    if total == 0:
        return 0
    return round(done / total * 100)


def greet(name: str, polite: bool = False) -> str:
    return f"{'Здравствуйте' if polite else 'Привет'}, {name}!"


# main.py
import random
from datetime import date
from utils import progress, greet

print(progress(3, 5))            # 60
print(greet("Павел", polite=True))
print(random.choice(["HTML", "Python", "SQL"]))
print(date.today().year)
```

## Практическое задание

1. Напишите функцию `is_palindrome(text)`, которая игнорирует регистр и пробелы. Проверьте её на строке «А роза упала на лапу Азора».
2. Вынесите функции в модуль `utils.py` и импортируйте их из другого файла.
3. С помощью модуля `random` напишите игру «угадай число от 1 до 100»: программа загадывает, пользователь угадывает, программа подсказывает «больше» или «меньше».
'''),
        ("Файлы и исключения", """
Программы должны сохранять результаты и переживать ошибки. Файл открывают функцией `open(путь, режим, encoding="utf-8")`: режим `"r"` — чтение, `"w"` — запись с очисткой, `"a"` — дозапись. Конструкция `with open(...) as f:` сама закроет файл, даже если внутри случится ошибка.

Читать можно целиком (`f.read()`), построчно (`for line in f`) или списком строк (`f.readlines()`). Для структурированных данных используйте модули `json` (словари и списки ↔ текст) и `csv` (таблицы).

Ошибки во время выполнения называются исключениями: `ValueError` при неверном преобразовании, `ZeroDivisionError`, `FileNotFoundError` и другие. Их перехватывают блоком `try / except`, а `finally` выполняется всегда. Ловите конкретные исключения, а не голое `except:` — иначе скроете настоящие баги.

## Пример

```
import json

def load_progress(path):
    try:
        with open(path, encoding="utf-8") as f:
            return json.load(f)
    except FileNotFoundError:
        return {}
    except json.JSONDecodeError:
        print("Файл повреждён, начинаем заново")
        return {}

progress = load_progress("progress.json")
progress["Python: первый язык"] = progress.get("Python: первый язык", 0) + 1

with open("progress.json", "w", encoding="utf-8") as f:
    json.dump(progress, f, ensure_ascii=False, indent=2)

try:
    n = int(input("Сколько уроков прошли сегодня? "))
except ValueError:
    print("Нужно ввести целое число")
```

## Практическое задание

1. Напишите скрипт, который спрашивает у пользователя заметку и дописывает её в файл `notes.txt` с текущей датой.
2. Прочитайте `notes.txt` и выведите количество строк и самую длинную заметку.
3. Сделайте программу устойчивой: если файла нет — создайте его, если пользователь ввёл пустую строку — попросите повторить.

## Итог курса

Вы знаете базовый синтаксис Python, коллекции, функции, модули и работу с файлами. Это основа для следующих курсов: «SQL и базы данных» и «Аналитика данных на Python».
"""),
    ],
    # -------------------------------------------------- 4. SQL и базы данных
    4: [
        ("Что такое реляционная БД", """
База данных — это организованное хранилище, из которого можно быстро и надёжно доставать информацию. В реляционных базах данные лежат в таблицах: строки — записи, столбцы — поля с заданным типом (число, текст, дата).

Таблицы связаны между собой через ключи. Первичный ключ (`PRIMARY KEY`) уникально определяет строку — обычно это столбец `id`. Внешний ключ (`FOREIGN KEY`) ссылается на первичный ключ другой таблицы: так в таблице курсов поле `teacher_id` указывает на конкретного преподавателя. Благодаря связям данные не дублируются: имя преподавателя хранится один раз.

Для общения с базой используется язык SQL. Популярные СУБД — PostgreSQL, MySQL, SQLite. Последняя хранит всю базу в одном файле и отлично подходит для обучения: она встроена в Python и не требует установки.

## Пример

```
CREATE TABLE teachers (
    id   INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    role TEXT NOT NULL
);

CREATE TABLE courses (
    id         INTEGER PRIMARY KEY,
    title      TEXT NOT NULL,
    hours      INTEGER NOT NULL,
    teacher_id INTEGER REFERENCES teachers(id)
);

INSERT INTO teachers (name, role) VALUES ('Павел Дорохов', 'Python и данные');
INSERT INTO courses (title, hours, teacher_id) VALUES ('SQL и базы данных', 30, 1);
```

## Практическое задание

1. Установите любой SQL-клиент (например, DB Browser for SQLite) или используйте онлайн-песочницу.
2. Создайте таблицы `students` (id, name, city) и `enrollments` (id, student_id, course_id, created_at) со связью через внешний ключ.
3. Добавьте по три записи в каждую таблицу. Подумайте, какие данные вы бы вынесли в отдельную таблицу, чтобы избежать дублирования.
"""),
        ("Запрос SELECT и фильтры", """
`SELECT` — самый частый запрос в SQL: он достаёт данные из таблицы. Минимальная форма — `SELECT столбцы FROM таблица`. Звёздочка `*` выбирает все столбцы, но в рабочем коде лучше перечислять нужные явно.

Условие `WHERE` фильтрует строки. В нём работают сравнения (`=`, `<>`, `>`, `<=`), логические `AND` / `OR` / `NOT`, проверка диапазона `BETWEEN`, список значений `IN (...)`, поиск по шаблону `LIKE 'Py%'` (где `%` — любые символы) и проверка пустых значений `IS NULL`.

`ORDER BY` сортирует результат (по умолчанию по возрастанию, `DESC` — по убыванию), `LIMIT` ограничивает число строк, `DISTINCT` убирает дубликаты. Порядок частей запроса фиксирован: SELECT → FROM → WHERE → ORDER BY → LIMIT.

## Пример

```
-- курсы начального уровня короче 40 часов, сначала самые короткие
SELECT title, level, hours
FROM courses
WHERE level = 'Начальный' AND hours < 40
ORDER BY hours;

-- всё, что связано с Python или SQL
SELECT title FROM courses
WHERE title LIKE '%Python%' OR title LIKE '%SQL%';

-- три самых длинных курса из двух направлений
SELECT title, hours FROM courses
WHERE category IN ('Аналитика', 'Дизайн')
ORDER BY hours DESC
LIMIT 3;

-- список направлений без повторов
SELECT DISTINCT category FROM courses ORDER BY category;
```

## Практическое задание

1. Выберите все курсы длительностью от 24 до 36 часов включительно, отсортированные по названию.
2. Найдите курсы, в названии которых есть слово «данных», и выведите только название и часы.
3. Получите пять последних записей на курсы (по полю `created_at`) из таблицы `enrollments`.
"""),
        ("JOIN и подзапросы", """
Данные разложены по таблицам, а отчёты нужны сводные: «название курса + имя преподавателя». Соединение таблиц делает `JOIN`: указываем вторую таблицу и условие связи через `ON`.

`INNER JOIN` (или просто `JOIN`) возвращает только строки, у которых нашлась пара в обеих таблицах. `LEFT JOIN` сохраняет все строки левой таблицы, а если пары нет — подставляет `NULL`. Это важно: курс без преподавателя при `INNER JOIN` пропадёт из отчёта, а при `LEFT JOIN` останется. Таблицам дают короткие псевдонимы (`courses c`), чтобы запрос был читаемым.

Подзапрос — это `SELECT` внутри другого запроса. Его используют в `WHERE ... IN (SELECT ...)`, как вычисляемый столбец в списке `SELECT` или как временную таблицу во `FROM`. Часто одну и ту же задачу можно решить и через `JOIN`, и через подзапрос — выбирайте более понятный вариант.

## Пример

```
-- курс и его преподаватель (курсы без преподавателя тоже останутся)
SELECT c.title, t.name AS teacher
FROM courses c
LEFT JOIN teachers t ON t.id = c.teacher_id;

-- курсы, на которые записан хотя бы один студент
SELECT title FROM courses
WHERE id IN (SELECT course_id FROM enrollments);

-- число записавшихся как вычисляемый столбец
SELECT c.title,
       (SELECT COUNT(*) FROM enrollments e WHERE e.course_id = c.id) AS students
FROM courses c
ORDER BY students DESC;
```

## Практическое задание

1. Выведите список уроков вместе с названием курса, к которому они относятся (`lessons` + `courses`).
2. Найдите преподавателей, у которых нет ни одного курса. Подсказка: `LEFT JOIN` и проверка `IS NULL`.
3. Перепишите второй запрос из примера через `JOIN` и `DISTINCT` и сравните результат.
"""),
        ("Агрегации и GROUP BY", """
Агрегатные функции сворачивают множество строк в одно значение: `COUNT` считает строки, `SUM` суммирует, `AVG` берёт среднее, `MIN` и `MAX` находят крайние значения. Без группировки они применяются ко всей выборке: `SELECT AVG(hours) FROM courses`.

`GROUP BY` делит строки на группы по значению столбца, и агрегат считается для каждой группы отдельно: например, число курсов в каждом направлении. Правило: в `SELECT` могут быть только столбцы из `GROUP BY` и агрегатные функции.

Чтобы отфильтровать уже посчитанные группы, используют `HAVING` — `WHERE` для этого не подходит, потому что срабатывает до группировки. Порядок выполнения запроса: FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY.

## Пример

```
-- сколько курсов и часов в каждом направлении
SELECT category,
       COUNT(*)            AS courses_n,
       SUM(hours)          AS total_hours,
       ROUND(AVG(hours), 1) AS avg_hours
FROM courses
GROUP BY category
ORDER BY courses_n DESC;

-- средняя оценка курсов, у которых не меньше двух отзывов
SELECT c.title, ROUND(AVG(f.rating), 1) AS avg_rating, COUNT(*) AS reviews
FROM feedback f
JOIN courses c ON c.id = f.course_id
GROUP BY c.id
HAVING COUNT(*) >= 2
ORDER BY avg_rating DESC;
```

## Практическое задание

1. Посчитайте, сколько уроков в каждом курсе, и выведите курсы с наибольшим числом уроков первыми.
2. Найдите преподавателя с максимальной суммарной длительностью курсов.
3. Выведите направления, в которых средняя длительность курса больше 30 часов.

## Итог курса

Вы умеете проектировать таблицы, выбирать и фильтровать данные, соединять таблицы и строить сводные отчёты. Этого хватает для большинства аналитических задач — продолжить можно на курсе «Аналитика данных на Python».
"""),
    ],
    # ---------------------------------------- 5. Аналитика данных на Python
    5: [
        ("Знакомство с pandas", """
pandas — главная библиотека Python для работы с табличными данными. Установите её командой `pip install pandas` и подключайте как `import pandas as pd`. Работать удобнее всего в Jupyter Notebook, где результат каждого шага виден сразу.

Основная структура — `DataFrame`: таблица со строками и именованными столбцами. Один столбец — это `Series`. Данные загружают из CSV (`pd.read_csv`), Excel, SQL или обычных словарей и списков.

Первые шаги с любым датасетом одинаковы: посмотреть верхние строки (`head()`), размер (`shape`), типы и пропуски (`info()`), базовую статистику (`describe()`). Столбцы выбирают по имени `df["hours"]`, строки фильтруют логическим условием `df[df["hours"] > 30]`, а `sort_values` сортирует таблицу.

## Пример

```
import pandas as pd

df = pd.DataFrame({
    "title": ["Основы HTML и CSS", "Python: первый язык", "SQL и базы данных", "UX/UI: основы дизайна"],
    "category": ["Веб-разработка", "Программирование", "Аналитика", "Дизайн"],
    "hours": [24, 40, 30, 28],
    "students": [120, 210, 95, 60],
})

print(df.head())
print(df.shape)              # (4, 4)
print(df["hours"].mean())    # 30.5

long_courses = df[df["hours"] >= 30].sort_values("students", ascending=False)
print(long_courses[["title", "students"]])
```

## Практическое задание

1. Установите pandas и Jupyter (`pip install pandas notebook`), запустите ноутбук.
2. Скачайте любой открытый CSV-датасет (например, с Kaggle) и загрузите его через `pd.read_csv`.
3. Выведите `head()`, `info()` и `describe()`. Запишите три наблюдения о данных: сколько строк, есть ли пропуски, какие столбцы числовые.
"""),
        ("Очистка данных", """
Аналитики говорят, что 80 % времени уходит на подготовку данных. Реальные таблицы содержат пропуски, дубликаты, опечатки в категориях, числа, записанные как текст, и даты в разных форматах. Пока это не исправлено, любой расчёт будет врать.

Пропуски находят через `df.isna().sum()`. Их либо удаляют (`dropna`), либо заполняют (`fillna`) — средним, медианой, нулём или значением «неизвестно» в зависимости от смысла столбца. Дубликаты убирает `drop_duplicates()`.

Типы приводят методами `astype`, `pd.to_numeric(errors="coerce")` и `pd.to_datetime`. Текстовые столбцы чистят через аксессор `.str`: `str.strip()`, `str.lower()`, `str.replace()`. Новые столбцы создаются простым присваиванием, а переименовать поля можно через `rename`.

## Пример

```
import pandas as pd

raw = pd.DataFrame({
    "course": ["Python ", "python", "SQL", None, "SQL"],
    "hours": ["40", "40", "30", "30", "тридцать"],
    "enrolled_at": ["2026-01-10", "2026-01-10", "10.02.2026", "2026-03-01", "2026-03-05"],
})

df = raw.copy()
df["course"] = df["course"].str.strip().str.capitalize()
df["hours"] = pd.to_numeric(df["hours"], errors="coerce")
df["enrolled_at"] = pd.to_datetime(df["enrolled_at"], format="mixed", dayfirst=True)

print(df.isna().sum())
df = df.dropna(subset=["course"])
df["hours"] = df["hours"].fillna(df["hours"].median())
df = df.drop_duplicates()
print(df)
```

## Практическое задание

1. В датасете из прошлого урока посчитайте пропуски по каждому столбцу и решите, что делать с каждым: удалить, заполнить или оставить.
2. Приведите все текстовые категории к единому регистру и уберите лишние пробелы. Проверьте `value_counts()` до и после.
3. Найдите столбец с датами, приведите его к типу `datetime` и добавьте столбцы «год» и «месяц».
"""),
        ("Группировки и сводные таблицы", """
Самые ценные выводы получаются из сравнения групп: какое направление популярнее, в каком месяце больше записей, у какого преподавателя выше оценки. В pandas за это отвечает `groupby` — прямой аналог `GROUP BY` из SQL.

Схема одна и та же: разбить (`groupby("category")`) → применить агрегат (`sum`, `mean`, `count`, `agg`) → объединить результат. Метод `agg` позволяет посчитать несколько метрик сразу и дать им имена. Результат — новый DataFrame, который можно сортировать и фильтровать.

Сводная таблица `pivot_table` — это группировка сразу по двум измерениям: строки и столбцы. Например, направления по строкам, уровни по столбцам, в ячейках — число студентов. Для «плоской» таблицы событий это самый быстрый способ увидеть картину целиком.

## Пример

```
import pandas as pd

df = pd.DataFrame({
    "category": ["Веб", "Веб", "Аналитика", "Аналитика", "Дизайн"],
    "level": ["Начальный", "Начальный", "Средний", "Средний", "Продвинутый"],
    "hours": [24, 36, 30, 48, 22],
    "students": [120, 90, 95, 70, 40],
})

by_cat = df.groupby("category").agg(
    courses=("hours", "count"),
    total_hours=("hours", "sum"),
    avg_students=("students", "mean"),
).sort_values("avg_students", ascending=False)
print(by_cat)

pivot = pd.pivot_table(df, index="category", columns="level",
                       values="students", aggfunc="sum", fill_value=0)
print(pivot)
```

## Практическое задание

1. Сгруппируйте ваш датасет по любой категории и посчитайте количество записей, сумму и среднее по числовому столбцу.
2. Постройте сводную таблицу по двум категориальным признакам. Какая комбинация встречается чаще всего?
3. Добавьте столбец с долей каждой группы в процентах от общего итога (подсказка: разделите на `sum()` и умножьте на 100).
"""),
        ("Графики и отчёты", """
Таблица с цифрами убеждает хуже, чем один правильный график. Библиотека matplotlib — стандарт визуализации в Python, а pandas умеет строить графики прямо из DataFrame методом `.plot()`.

Выбирайте тип по задаче: столбчатая диаграмма (`kind="bar"`) — сравнение категорий, линейный график (`kind="line"`) — динамика во времени, гистограмма (`kind="hist"`) — распределение значений, точечная (`kind="scatter"`) — связь двух показателей. Круговые диаграммы используйте редко: их трудно читать при более чем трёх-четырёх долях.

У любого графика должны быть заголовок, подписи осей и единицы измерения. Сохраняйте картинки через `plt.savefig("report.png", dpi=150)`. Итоговый отчёт — это не набор графиков, а история: вопрос → данные → график → вывод → рекомендация.

## Пример

```
import pandas as pd
import matplotlib.pyplot as plt

df = pd.DataFrame({
    "month": ["Янв", "Фев", "Мар", "Апр", "Май", "Июн"],
    "enrollments": [80, 95, 130, 120, 160, 190],
})

ax = df.plot(x="month", y="enrollments", kind="line", marker="o", legend=False)
ax.set_title("Записи на курсы по месяцам")
ax.set_xlabel("Месяц")
ax.set_ylabel("Число записей")
plt.tight_layout()
plt.savefig("enrollments.png", dpi=150)
plt.show()

top = pd.Series({"Python": 210, "HTML/CSS": 120, "SQL": 95, "UX/UI": 60})
top.sort_values().plot(kind="barh", title="Популярность курсов, студентов")
plt.tight_layout()
plt.show()
```

## Практическое задание

1. Постройте по вашему датасету три разных графика: сравнение категорий, динамику во времени и распределение числового признака.
2. Подпишите оси и заголовки, сохраните графики в PNG.
3. Соберите мини-отчёт на одну страницу: три графика, по одному выводу к каждому и одна общая рекомендация. Это ваш первый аналитический артефакт для портфолио.

## Итог курса

Вы прошли полный цикл аналитика: загрузка → очистка → группировки → визуализация → выводы. Дальше стоит изучить статистику, A/B-тесты и BI-инструменты.
"""),
    ],
    # ---------------------------------------------- 6. UX/UI: основы дизайна
    6: [
        ("Что делает дизайнер", """
Дизайнер интерфейсов не «рисует красивые картинки» — он решает задачи пользователей и бизнеса через продукт. UX (user experience) отвечает за то, насколько удобно и понятно пользоваться сервисом; UI (user interface) — за визуальный слой: цвета, шрифты, кнопки, состояния.

Типичный процесс: понять задачу и аудиторию → изучить конкурентов и пользователей → набросать сценарии и структуру → сделать черновые макеты (вайрфреймы) → проработать визуал → проверить на людях → передать в разработку и поддерживать. На каждом этапе дизайнер разговаривает с менеджерами, разработчиками и пользователями — коммуникация не менее важна, чем владение инструментами.

Главный принцип — ясность: пользователь должен понимать, где он, что может сделать и что произойдёт после действия. Всё, что не помогает этому, — лишнее.

## Пример

Разберём страницу курса на «Курсограде»: заголовок и теги отвечают на вопрос «что это», описание и список уроков — «что я получу», блок справа с одной заметной кнопкой — «что сделать дальше». Отзывы снижают тревогу перед записью, а полоса прогресса даёт ощущение движения. Каждый элемент решает конкретную задачу.

## Практическое задание

1. Выберите любое приложение, которым пользуетесь каждый день. Опишите одним предложением, какую задачу оно решает.
2. Найдите на его главном экране три элемента и объясните, зачем каждый из них нужен. Есть ли элемент, который можно убрать без потери?
3. Опишите один момент, где вы задумались «а что тут нажать?». Это и есть UX-проблема — предложите, как её решить.
"""),
        ("Композиция и сетка", """
Композиция — это порядок элементов на экране, который направляет взгляд. Пользователь не читает интерфейс, а сканирует его, поэтому важна визуальная иерархия: главное — крупнее и контрастнее, второстепенное — мельче и спокойнее.

Базовые принципы: близость (связанные элементы стоят рядом), выравнивание (элементы выстроены по общим линиям), контраст (важное выделяется размером, цветом, весом), повторение (одинаковые вещи выглядят одинаково) и воздух — пустое пространство, которое даёт элементам «дышать».

Сетка помогает соблюдать всё это системно. Для веба стандарт — 12-колоночная сетка с межколонниками 16–24px, а размеры и отступы задают кратными 8px (8, 16, 24, 32…). Такой ритм делает макет аккуратным даже без «чувства прекрасного».

## Пример

Карточка курса в каталоге построена по этим правилам: теги, заголовок и описание сгруппированы вместе (близость), все карточки одинаковой структуры (повторение), заголовок крупнее и жирнее описания (контраст), внутренние отступы 20px и промежутки 18px создают воздух, а сама сетка карточек выровнена по колонкам.

## Практическое задание

1. Сделайте скриншот любой страницы и проведите линии по краям элементов. Найдите места, где выравнивание нарушено.
2. Возьмите лист бумаги или Figma и набросайте макет карточки товара или курса, используя только прямоугольники и отступы, кратные 8px.
3. Сделайте два варианта одного экрана: «плотный» и «с воздухом». Покажите кому-нибудь и спросите, какой понятнее.
"""),
        ("Цвет и типографика", """
Текст — это 90 % интерфейса, поэтому типографика решает, будет ли продуктом удобно пользоваться. Для экрана берите один-два шрифта, базовый размер 16px и межстрочный интервал 1.4–1.6. Стройте шкалу размеров (например, 14 / 16 / 20 / 24 / 32) и используйте только её. Длина строки текста — 45–75 символов, иначе глазу трудно возвращаться к началу.

Цвет в интерфейсе несёт смысл: акцентный цвет — для главных действий, нейтральные серые — для текста и фонов, зелёный / красный / жёлтый — для статусов. Правило 60/30/10: 60 % нейтрального фона, 30 % вторичного, 10 % акцента. Не кодируйте информацию только цветом — добавляйте иконку или текст.

Обязательно проверяйте контраст текста и фона: по стандарту WCAG для обычного текста нужно соотношение не ниже 4.5:1. Это не формальность — так интерфейс могут читать люди с ослабленным зрением и на экранах под солнцем.

## Пример

Палитра «Курсограда»: тёмно-синий текст `#16203a` на светлом фоне `#f4f6fb`, акцент `#3457d5` для кнопок и ссылок, приглушённый серый `#67718c` для второстепенного текста, зелёный `#17966b` для статуса «пройден». Заголовки — 32 и 23px, основной текст — 16px с интервалом 1.6.

## Практическое задание

1. Соберите палитру для своего проекта: фон, текст, акцент, вторичный текст, цвет успеха и ошибки. Проверьте контраст пар «текст / фон» любым онлайн-чекером.
2. Постройте типографическую шкалу из пяти размеров и оформите ею страницу статьи: заголовок, подзаголовок, абзацы, подпись.
3. Найдите на любом сайте кнопку, которая «спорит» с главным действием за внимание. Предложите, как её приглушить.
"""),
        ("Прототип в Figma", """
Figma — бесплатный браузерный редактор, в котором сегодня делают большинство интерфейсов. Основные инструменты: фреймы (артборды экранов), фигуры, текст, компоненты (переиспользуемые элементы вроде кнопок) и автолейаут — аналог флексбокса, который сам расставляет отступы.

Прототипирование идёт от простого к сложному. Сначала — низкодетализированный вайрфрейм серыми блоками, чтобы проверить структуру и сценарий. Потом — макет в цвете с реальными текстами. Затем во вкладке Prototype экраны связывают переходами, и получается кликабельный прототип, который можно показать пользователям до написания кода.

Хорошая привычка — сразу заводить стили цветов и текста и собирать кнопки и поля как компоненты с вариантами (обычная, при наведении, неактивная). Тогда правки вносятся в одном месте.

## Пример

Прототип сценария «Запись на курс» состоит из четырёх экранов: каталог → страница курса → экран входа (если пользователь не авторизован) → страница курса с состоянием «Вы записаны». Переходы: клик по карточке, клик по кнопке «Записаться», отправка формы. На каждом экране одна главная кнопка в акцентном цвете.

## Практическое задание

1. Зарегистрируйтесь в Figma и создайте файл с фреймами 1440×900 (десктоп) и 390×844 (мобильный).
2. Сверстайте вайрфрейм страницы курса: заголовок, описание, список уроков, кнопка записи. Используйте автолейаут для списка.
3. Сделайте второй экран «Вы записаны» и свяжите экраны переходом по кнопке. Запустите режим презентации и пройдите сценарий.

## Итог курса

Вы понимаете роль дизайнера, знаете принципы композиции, цвета и типографики и умеете собрать кликабельный прототип. Чтобы проверять решения на реальных людях, пройдите курс «Исследование пользователей».
"""),
    ],
    # ----------------------------------------- 7. Исследование пользователей
    7: [
        ("Планирование исследования", """
Исследование пользователей начинается не с анкеты, а с вопроса: что мы хотим узнать и какое решение примем по результату. Без сформулированной цели вы соберёте много интересных фактов и ни одного полезного.

Запишите цель («понять, почему студенты бросают курс после второго урока»), гипотезы («уроки слишком длинные», «непонятно, что делать дальше») и вопросы исследования. Затем выберите метод: качественные методы (интервью, наблюдение, юзабилити-тесты) отвечают на вопрос «почему», количественные (опросы, аналитика) — «сколько». Для новых гипотез начинают с качественных.

Определите аудиторию и критерии отбора (например: записались на курс за последний месяц и прошли не больше двух уроков), способ рекрутинга и размер выборки — для интервью обычно достаточно 5–8 человек на сегмент. Составьте план: сроки, роли, где храним записи, как будем анализировать. Не забудьте согласие на запись и обработку данных.

## Пример

```
Цель: понять причины отказа от курса на ранних уроках.
Решение по итогам: переработать первые два урока и онбординг.
Гипотезы:
  1. Студенты не понимают, сколько времени займёт урок.
  2. Практические задания кажутся слишком сложными.
  3. Нет ощущения прогресса.
Метод: 6 глубинных интервью по 40 минут + просмотр записей сессий.
Аудитория: записались 2–6 недель назад, пройдено не больше 2 уроков.
Сроки: рекрутинг — неделя, интервью — неделя, анализ — 3 дня.
```

## Практическое задание

1. Выберите продукт (можно «Курсоград») и сформулируйте одну цель исследования и решение, которое примете по итогам.
2. Запишите три гипотезы и для каждой — метод, которым её можно проверить.
3. Опишите портрет респондента и составьте скринер из пяти вопросов, отсекающий неподходящих людей.
"""),
        ("Проведение интервью", """
Глубинное интервью — разговор один на один, в котором вы изучаете опыт, мотивы и проблемы человека. Его главная ловушка — люди плохо предсказывают своё поведение и склонны говорить то, что от них ждут. Поэтому спрашивайте о прошлом опыте, а не о будущих намерениях: не «купили бы вы», а «расскажите, как в последний раз выбирали курс».

Заранее подготовьте гайд — список тем и открытых вопросов, но не читайте его как анкету. Начните с лёгких вопросов о контексте, затем переходите к ключевым темам, в конце — резюме и благодарность. Задавайте открытые вопросы («как», «почему», «расскажите»), избегайте наводящих («правда ведь неудобно?») и закрытых с ответом «да / нет». Держите паузы: после ответа помолчите пару секунд — люди часто добавляют самое важное.

Ведите интервью вдвоём: один спрашивает, второй фиксирует. Сразу после разговора выпишите главные наблюдения и цитаты, пока они свежи. Расшифровки и заметки кладите в общую таблицу — она пригодится для анализа.

## Пример

```
Плохо:  «Вам ведь не хватает мотивации доучиваться?»
Хорошо: «Расскажите, как проходило ваше обучение на последнем курсе.
         Что происходило, когда вы делали перерыв?»

Плохо:  «Вы бы пользовались напоминаниями об уроках?»
Хорошо: «Как вы обычно вспоминаете о том, что нужно позаниматься?
         Приведите пример за последнюю неделю».
```

## Практическое задание

1. Напишите гайд интервью на 8–10 открытых вопросов по своей цели из прошлого урока.
2. Проведите одно пробное интервью с другом или коллегой (20–30 минут) и запишите его с разрешения.
3. Переслушайте запись и отметьте: сколько раз вы задали наводящий вопрос, перебили респондента или не выдержали паузу. Исправьте гайд.
"""),
        ("Юзабилити-тестирование", """
Юзабилити-тест проверяет не мнение, а поведение: справится ли человек с задачей в интерфейсе. Участнику дают реалистичный сценарий («Вы хотите изучить SQL. Найдите подходящий курс и запишитесь») и наблюдают, где он застревает, что не замечает и как это комментирует.

Для теста достаточно пяти участников — они выявляют около 80 % серьёзных проблем. Подготовьте 3–5 задач, от простой к сложной, и формулируйте их без подсказок: не «нажмите кнопку "Записаться"», а «сделайте так, чтобы курс появился в вашем кабинете». Попросите участника думать вслух и не помогайте, пока он окончательно не сдастся — иначе вы не увидите проблему.

Фиксируйте для каждой задачи: выполнена ли, за сколько, с какими ошибками, что говорил участник. После всех сессий сведите наблюдения в таблицу, оцените серьёзность проблем (блокирует / мешает / косметика) и приоритизируйте по частоте и тяжести. Результат — короткий отчёт с находками, цитатами, скриншотами и рекомендациями.

## Пример

```
Задача 2: «Найдите курс по SQL и запишитесь на него».
Участник 1 — выполнил, 1:40, сначала искал раздел «Обучение» в меню.
Участник 2 — выполнил, 0:55, без затруднений.
Участник 3 — не выполнил: не понял, что нужно войти в аккаунт.
Участник 4 — выполнил, 2:30, не заметил кнопку «Записаться» на узком экране.
Участник 5 — выполнил, 1:10.

Находка: на узких экранах блок записи уходит вниз — 1 из 5 не нашёл кнопку (мешает).
Находка: требование входа неочевидно — 1 из 5 не завершил задачу (блокирует).
```

## Практическое задание

1. Составьте сценарий из четырёх задач для тестирования «Курсограда» или другого сайта.
2. Проведите тест с тремя людьми, замеряя время и фиксируя проблемы.
3. Оформите отчёт на одну страницу: таблица результатов, топ-3 проблем с оценкой серьёзности и рекомендации по исправлению.

## Итог курса

Вы умеете спланировать исследование, провести интервью и юзабилити-тест и превратить наблюдения в решения. Это ключевой навык продуктового дизайнера и менеджера.
"""),
    ],
    # ---------------------------------------------------- 8. Английский для IT
    8: [
        ("Лексика разработчика", """
Технический английский проще общего: словарный запас ограничен, конструкции повторяются. Начните с базовых глаголов действий, которые встречаются в каждой задаче: to implement (реализовать), to deploy (выкатить), to debug (отлаживать), to refactor (переписать без изменения поведения), to merge (слить ветки), to roll back (откатить).

Существительные вокруг задач и кода: feature, bug, issue, pull request, release, environment, dependency, endpoint. Обратите внимание на слова, которые звучат знакомо, но значат другое: actual — «фактический», а не «актуальный»; to control — «управлять», а не «контролировать»; data — во множественном числе.

Полезная привычка — учить слова не списком, а фразами из реальных сообщений: «I've fixed the bug in the login form», «The build is failing on CI», «Let's merge this after review».

## Пример

```
implement a feature    — реализовать функциональность
fix a bug              — исправить ошибку
open a pull request    — открыть запрос на слияние
request changes        — запросить правки (при ревью)
deploy to production   — выкатить на прод
roll back a release    — откатить релиз
the tests are failing  — тесты падают
it works as expected   — работает как задумано
out of scope           — вне рамок задачи
```

## Практическое задание

1. Выпишите из любого репозитория на GitHub 10 заголовков коммитов или задач и переведите их. Отметьте незнакомые глаголы.
2. Составьте пять предложений о своей текущей учёбе или работе, используя слова из списка: например, «I'm implementing a progress bar for the course page».
3. Заведите словарь-таблицу «слово — пример фразы — перевод» и добавляйте туда по пять выражений в день в течение недели.
"""),
        ("Чтение документации", """
Документация — основной источник знаний разработчика, и почти вся она на английском. Хорошая новость: её не нужно читать как роман. Научитесь сканировать структуру: Overview / Getting started (что это и как начать), Installation, Usage / Guide (примеры), API Reference (полный список функций и параметров), FAQ / Troubleshooting.

Типичные конструкции документации: «This method returns…», «Raises ValueError if…», «Note that…», «deprecated since version…», «optional», «defaults to…». Модальные глаголы важны: must — обязательно, should — рекомендуется, may — можно. В стандартах RFC они пишутся заглавными и имеют строгий смысл.

Читайте примеры кода раньше текста, затем возвращайтесь к описанию параметров. Если непонятно слово — не переводите весь абзац, найдите только его: контекст обычно подскажет остальное.

## Пример

```
pandas.DataFrame.fillna(value=None, method=None, axis=None, inplace=False)

Fill NA/NaN values using the specified method.

Parameters
  value : scalar, dict, Series, or DataFrame
      Value to use to fill holes. Alternately, a dict of values specifying
      which value to use for each column.
  inplace : bool, default False
      If True, fill in-place. Note: this will modify any other views on this object.

Returns
  DataFrame or None — object with missing values filled or None if inplace=True.
```

Разбор: «Fill NA values» — заполнить пропуски; «default False» — по умолчанию выключено; «Note: this will modify…» — предупреждение о побочном эффекте; «Returns … or None if inplace=True» — важная деталь про возвращаемое значение.

## Практическое задание

1. Откройте документацию любой библиотеки, которой пользуетесь (Flask, pandas, React), и найдите разделы Getting started и API Reference.
2. Выберите одну функцию и письменно опишите по-русски: что делает, какие параметры обязательны, что возвращает, какие есть предупреждения.
3. Выпишите 10 повторяющихся конструкций («returns», «raises», «deprecated», «optional»…) и запомните их — они покрывают большую часть любой документации.
"""),
        ("Митинги и созвоны", """
Устное общение пугает больше всего, но рабочие созвоны — самый предсказуемый жанр. Дейли-стендап всегда строится по одной схеме: что сделал вчера, что планирую сегодня, что мешает. Подготовьте три предложения заранее — и вы уже участвуете.

Полезные фразы для управления разговором: «Sorry, could you repeat that?» (переспросить), «Let me make sure I understand…» (уточнить), «I'll get back to you on that» (вернусь с ответом позже), «Can we take this offline?» (обсудим отдельно), «I'm blocked by…» (меня блокирует). Переспрашивать нормально — хуже промолчать и сделать не то.

Не стремитесь к идеальной грамматике: коллеги ценят ясность и краткость. Говорите короткими предложениями, называйте задачи по номерам или заголовкам, подтверждайте договорённости в чате после звонка: «To sum up: I'll fix the login bug by Thursday, Anna will review».

## Пример

```
Daily standup:
— Yesterday I finished the course page layout and opened a PR.
— Today I'm going to work on the progress bar and fix review comments.
— I'm blocked: I need access to the staging database.

Уточнение:
— Sorry, I didn't catch the last part. Do you mean the mobile version only?
— Just to confirm: we're releasing on Friday, right?

Завершение:
— Sounds good. I'll send a summary in the chat.
```

## Практическое задание

1. Напишите и произнесите вслух свой стендап на три предложения о текущей задаче. Запишите себя на диктофон и послушайте.
2. Посмотрите любую техническую презентацию на английском (например, доклад с конференции) и выпишите 10 фраз, которыми спикер управляет вниманием: «let's move on», «the key point is»…
3. Найдите партнёра и проведите 10-минутный созвон по сценарию: один рассказывает о проблеме, второй задаёт уточняющие вопросы и подводит итог.
"""),
        ("Письма и код-ревью", """
Письменное общение в IT-команде — это письма, сообщения в чате, описания задач и комментарии к коду. Общие правила: одна мысль — одно сообщение, главное — в первой строке, конкретный вопрос или просьба — в конце. Тема письма должна отвечать на вопрос «о чём это»: «Login bug on mobile Safari — need review by Thursday».

Код-ревью требует особой деликатности: вы комментируете код, а не человека. Формулируйте замечания как вопросы и предложения: «What do you think about extracting this into a function?» вместо «This is wrong». Различайте блокирующие замечания и пожелания — многие команды помечают их «nit:» (мелочь, необязательно). Хвалите хорошие решения — это тоже часть ревью.

Полезные шаблоны для ответов: «Good catch, fixed» (спасибо, исправил), «I'd rather keep it as is because…» (предпочту оставить, потому что…), «Done in the latest commit» (готово в последнем коммите), «Could you elaborate?» (поясните подробнее).

## Пример

```
Subject: Course page — progress bar PR ready for review

Hi Anna,

I've opened PR #42 with the progress bar for the course page.
Main changes:
  - new progress component;
  - course_progress() helper moved to utils.

Could you review it by Thursday? The release is planned for Friday.

Thanks,
Pavel

---
Review comments:
  nit: consider renaming `pct` to `percent` for readability.
  Should we handle the case when a course has no lessons? Right now it divides by zero.
  Nice use of a helper here — much cleaner than the previous version.
```

## Практическое задание

1. Напишите письмо коллеге с просьбой проверить вашу работу: тема, контекст в двух строках, конкретная просьба со сроком.
2. Возьмите любой фрагмент кода (свой или из открытого проекта) и напишите три комментария ревью: один блокирующий, один «nit» и одну похвалу.
3. Ответьте на три чужих замечания разными способами: согласие с исправлением, аргументированный отказ, просьба уточнить.

## Итог курса

Вы освоили базовую лексику, научились читать документацию, участвовать в созвонах и вести деловую переписку. Продолжайте практиковаться ежедневно: читайте changelog любимых библиотек и пишите коммиты на английском.
"""),
    ],
}
